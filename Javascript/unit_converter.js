const readline = require('readline-sync');

function ft_converter(user_num) {
    const ft_out = (a * 0.3048)
    return ft_out
}

user_num = prompt("Enter a number in feet that you want converted to meters:");

console.log(ft_converter(a))